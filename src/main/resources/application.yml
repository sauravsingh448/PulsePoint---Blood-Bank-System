server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bloodbankdb?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password: Root@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # auto create/update tables, use cautiously in prod
    show-sql: true       # logs SQL queries in console
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  application:
    name: bloodbank-project


  mail:
    host: smtp.gmail.com
    port: 587
    username: sauravsinghk842@gmail.com
    password: djgdodjryczyiu
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true








